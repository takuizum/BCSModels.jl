# MCMC vs. Mean-field CAVI for BCSM — empirical comparison

Empirical results comparing the closed-form Gibbs sampler and the new
mean-field CAVI inference engines from this package. Both engines are run
on the same simulated data, with the same priors. Generated by
[`scripts/sim_mcmc_vs_vb.jl`](../scripts/sim_mcmc_vs_vb.jl) and aggregated
by [`scripts/analyze_results.jl`](../scripts/analyze_results.jl). The raw
CSV is at `results/sim_reps20.csv`.

## Experimental design

- 20 simulated replicates per cell.
- **MCMC**: Gibbs with `niter = 1000` after `burnin = 500` (closed-form
  conditionals; see [`derivations.md`](derivations.md)).
- **VB**: mean-field CAVI with `maxiter = 400`, `tol = 1e-7`. 95% credible
  intervals from the Gaussian approximation `m_θ ± 1.96·√v_θ`.
- IRT-BCSM cells (single covariance layer): `N ∈ {200, 500, 1000}`,
  `K ∈ {10, 20}`, true `θ ∈ {0.2, 0.4, 0.6}`.
- Testlet-BCSM cells (multi-layer): `T ∈ {3, 4}` testlets, true `θ_t` in
  `[0.15, 0.5]`.

## Results

### Aggregate (means across the 17 cells, each averaging 20 replicates)

| Metric                          | MCMC      | VB        |
|---------------------------------|-----------|-----------|
| bias                            | **+0.013** | **−0.117** |
| \|bias\|                        | **0.014** | **0.117** |
| RMSE                            | **0.061** | **0.128** |
| posterior SD (avg of replicates) | 0.060     | 0.025     |
| **sd ratio (VB / MCMC)** median | —         | **0.414** |
| **95% CI coverage**             | **0.94**  | **0.18**  |
| wall time (mean per fit)        | 0.65 s    | 0.06 s    |
| **speedup (MCMC / VB)** median  | —         | **11.5×** |

### How the user's narrative scores against the data

> *"パフォーマンス的にはMCMCの方が上だが、VBも肉薄している。VBは一部
> バイアスがあり、事後SDに関して過小評価する傾向があるが実用的である"*

| Sub-claim | Verdict | Evidence |
|---|---|---|
| MCMC performs better | **✓ confirmed** | mean \|bias\| 8× smaller; RMSE 2× smaller; coverage at nominal 0.94 vs. nominal 0.95. |
| VB is close ("肉薄") | **partial — true for single-layer, not for multi-layer** | IRT-BCSM (single layer): RMSE gap 0.03–0.07. Testlet-BCSM: VB underestimates θ_t by 0.10–0.22 — 30–45% of truth. |
| VB has *some* bias | **✓ confirmed but stronger than implied** | bias is systematic (always negative) and large enough to dominate RMSE in every multi-layer cell. |
| VB underestimates posterior SD | **✓ strongly confirmed** | median sd_ratio = 0.41 across all 17 cells; sd_ratio < 1 in every cell. |
| VB is practically useful | **✗ refuted for inference, ✓ confirmed for point-estimate speed** | 95% CI coverage collapses to 0.18 (far below nominal). Speedup is 9–20× though, so VB is useful as a *fast initialiser* or for *bulk point-estimate screens*, not for credible-interval inference. |

### Per-cell table (truth columns are the data-generating θ)

```
model | N | K | param | truth | bias_MC | bias_VB | rmse_MC | rmse_VB | sd_MC | sd_VB | sd_ratio | width_MC | width_VB | cov_MC | cov_VB | t_MC | t_VB | speedup
---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---
IRT | 200 | 10  | θ  | 0.20 | +0.025 | −0.044 | 0.054 | 0.058 | 0.049 | 0.026 | 0.52 | 0.189 | 0.101 | 0.90 | 0.45 | 0.156 | 0.008 | 19.9×
IRT | 200 | 10  | θ  | 0.40 | +0.046 | −0.066 | 0.100 | 0.096 | 0.077 | 0.044 | 0.57 | 0.299 | 0.171 | 0.85 | 0.55 | 0.157 | 0.009 | 17.8×
IRT | 200 | 10  | θ  | 0.60 | +0.041 | −0.113 | 0.104 | 0.135 | 0.104 | 0.059 | 0.57 | 0.404 | 0.231 | 0.90 | 0.40 | 0.161 | 0.010 | 16.7×
IRT | 500 | 10  | θ  | 0.20 | +0.009 | −0.058 | 0.031 | 0.063 | 0.030 | 0.015 | 0.51 | 0.118 | 0.060 | 0.95 | 0.15 | 0.383 | 0.020 | 19.5×
IRT | 500 | 10  | θ  | 0.40 | +0.009 | −0.097 | 0.043 | 0.104 | 0.046 | 0.026 | 0.55 | 0.179 | 0.100 | 0.95 | 0.10 | 0.376 | 0.020 | 18.7×
IRT | 500 | 10  | θ  | 0.60 | +0.015 | −0.133 | 0.072 | 0.145 | 0.063 | 0.036 | 0.57 | 0.245 | 0.141 | 0.95 | 0.20 | 0.386 | 0.023 | 16.5×
IRT | 1000| 20  | θ  | 0.40 | +0.003 | −0.055 | 0.028 | 0.062 | 0.025 | 0.018 | 0.70 | 0.097 | 0.069 | 0.95 | 0.30 | 1.489 | 0.127 | 11.7×
TLT | 500 | 12  | θ1 | 0.20 | +0.018 | −0.089 | 0.069 | 0.103 | 0.064 | 0.023 | 0.36 | 0.252 | 0.090 | 0.95 | 0.20 | 0.438 | 0.038 | 11.5×
TLT | 500 | 12  | θ1 | 0.40 | −0.003 | −0.196 | 0.084 | 0.209 | 0.081 | 0.029 | 0.35 | 0.315 | 0.113 | 0.90 | 0.05 | 0.438 | 0.040 | 11.0×
TLT | 500 | 12  | θ2 | 0.20 | +0.013 | −0.098 | 0.065 | 0.108 | 0.063 | 0.022 | 0.36 | 0.243 | 0.088 | 0.95 | 0.20 | 0.438 | 0.040 | 11.0×
TLT | 500 | 12  | θ2 | 0.40 | +0.001 | −0.201 | 0.068 | 0.212 | 0.086 | 0.028 | 0.33 | 0.336 | 0.112 | 0.95 | 0.00 | 0.438 | 0.038 | 11.5×
TLT | 500 | 12  | θ3 | 0.30 | +0.000 | −0.132 | 0.064 | 0.144 | 0.069 | 0.026 | 0.39 | 0.266 | 0.104 | 1.00 | 0.15 | 0.438 | 0.038 | 11.5×
TLT | 500 | 12  | θ3 | 0.50 | −0.005 | −0.218 | 0.093 | 0.232 | 0.089 | 0.034 | 0.38 | 0.348 | 0.132 | 0.95 | 0.05 | 0.438 | 0.040 | 11.0×
TLT | 1000| 20  | θ1 | 0.25 | +0.018 | −0.112 | 0.055 | 0.117 | 0.041 | 0.015 | 0.37 | 0.158 | 0.059 | 0.90 | 0.00 | 1.472 | 0.158 | 9.3×
TLT | 1000| 20  | θ2 | 0.35 | +0.016 | −0.138 | 0.044 | 0.146 | 0.046 | 0.018 | 0.40 | 0.178 | 0.072 | 0.95 | 0.05 | 1.472 | 0.158 | 9.3×
TLT | 1000| 20  | θ3 | 0.45 | +0.003 | −0.174 | 0.039 | 0.180 | 0.051 | 0.021 | 0.41 | 0.202 | 0.083 | 1.00 | 0.00 | 1.472 | 0.158 | 9.3×
TLT | 1000| 20  | θ4 | 0.15 | +0.007 | −0.062 | 0.028 | 0.067 | 0.031 | 0.013 | 0.42 | 0.121 | 0.050 | 0.90 | 0.15 | 1.472 | 0.158 | 9.3×
```

## Conclusion

**The user's framing is partly confirmed and partly refuted by the data.**

What the experiment confirms:

- MCMC clearly outperforms VB on every accuracy metric (bias, RMSE, CI
  coverage).
- VB carries a systematic *negative* bias on every covariance component:
  it under-estimates `θ` consistently, with the bias growing in `θ_true`
  and in the number of additive layers.
- VB severely under-estimates the posterior SD — the median variational
  SD is **41 % of the Gibbs posterior SD**, and the variational 95 %
  credible interval covers the truth only **18 %** of the time
  (vs. the nominal 94 % for MCMC).

What the experiment refutes or qualifies:

- "VB is close to MCMC" only holds for the **single-layer IRT-BCSM at
  small θ** (e.g. RMSE 0.03 vs 0.03 at `N=500, θ=0.2`). For testlet-BCSM
  with multiple layers and θ near 0.5, **VB is off by 40–45 % of the
  truth** — that is not "close" in any operational sense.
- "VB is practically useful" is true if "practical" means **point-estimate
  screening at 10× speed**, but false if it means **valid uncertainty
  quantification** — the credible intervals are not usable for inference.

So the working summary should read something like:

> *MCMC is the reference; VB matches it well only for simple
> single-layer BCSM with weak dependence. For practically relevant
> testlet settings VB shows a systematic 30 – 45 % negative bias on the
> covariance components and recovers only ~18 % nominal-95 % coverage.
> VB is 10–20× faster and useful as a screening tool, but not as a
> stand-alone inference engine.*

## Why this happens — and what would fix it

The mean-field factorisation `q(z) = ∏_{i,j} q(z_{ij})` removes the
within-testlet correlation between latent utilities. That correlation is
exactly what carries the signal for `θ_t`, so its expectation `E_q[Q_t]`
in the CAVI update for `q(θ_t)` is systematically too small, biasing
`E_q[θ_t]` downward and shrinking `Var_q[θ_t]` proportionally.

Two concrete upgrades likely close most of the gap:

1. **Block-structured variational family** `q(z_i) = N_K(μ_i, S_i)` with
   `S_i` parameterised by the same additive BCSM structure as the model.
   This preserves the within-testlet correlation under `q` and removes
   the dominant bias source. The CAVI for `q(θ_t)` then has the same
   sufficient statistic the Gibbs uses, so we expect bias to drop to the
   MCMC level.

2. **Importance-weighted ELBO** (Cho et al., 2024, *Psychometrika*) to
   correct the residual bias from any remaining mean-field assumptions.

Both are natural research follow-ups; neither is implemented in this
release.
